apiVersion: v1
kind: Service

metadata:
  name: redis

spec:
  selector:
    app: redis-link-service-to-redis-app-label

  ports:
    - port: {{ .Values.redis.port }}
      targetPort: {{ .Values.redis.port }}
